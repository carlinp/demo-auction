spring:
    datasource:
        url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
        driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    liquibase:
        change-log: db/changelog/db.changelog-master.yaml

application.datamode: jdbc
